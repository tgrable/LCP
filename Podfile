# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Parse_LCP' do
  # Uncomment the next line if you're using Swift or would like to use dynamic frameworks
  # use_frameworks!

  # Pods for Parse_LCP
  pod 'Parse'

  target 'Parse_LCPTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
